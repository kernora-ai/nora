# Free/Lite open-core runtime baseline (kernora-ai/nora).
# Keep this list minimal — paid-tier / optional deps stay private.
anthropic>=0.40.0
litellm>=1.35.0
flask>=3.0.0
mcp>=1.0.0
tomli>=2.0.0; python_version < "3.11"
PyYAML>=6.0
